// function logout() {
   // location.assign("/bellaGroup/public/logout");
// }

// var timer;
// function start() {
  // timer = setTimeout(function(){logout()}, 10000); //30 menit mouse tidak bergerak akan logout otomatis
// }

// jQuery(document).ready(function() {
	// start();
  
	// jQuery('body').onmousemove(function() {
		// clearTimeout(timer);
		// start();
	// });
	
	// jQuery('body').onclick(function() {
		// clearTimeout(timer);
		// start();
	// });
	
	// jQuery('body').ondblclick(function() {
		// clearTimeout(timer);
		// start();
	// });
	
	// jQuery('body').onmousedown(function() {
		// clearTimeout(timer);
		// start();
	// });

  // jQuery('body').mouseenter(function() {
     // clearTimeout(timer);
  // }).mouseleave(function(e) {
     // var pageX = e.pageX || e.clientX,
         // pageY = e.pageY || e.clientY;

      // if(pageX <= 0 || pageY <= 0) {
        // start();
      // }else {
        // clearTimeout(timer);
      // }
  // });
// });